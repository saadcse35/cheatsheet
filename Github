git init

git remote add origin "---------link--------"

git add -A 

git commit -m "commit comment"

git push -u origin main


### Force to Push command
git push -f origin main



### Generate SSH key Command---
ssh-keygen -o -t rsa -C "your_email@gmail.com"
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa
clip < ~/.ssh/id_rsa.pub
### then paste it to github settings & save it.


### Primary Command
echo "# test-for-learn" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:saadcse35/test-for-learn.git
git push -u origin main

git push --force origin main



### Alternative Primary Command
git remote add origin git@github.com:saadcse35/test-for-learn.git
git branch -M main
git push -u origin main


### Command of git stash
git stash


### git clean remote origin
git remote remove origin

### git branch name change (master to main)
git branch -m master main
